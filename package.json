{
  "name": "weather-dashboard",
  "version": "1.0.0",
  "description": "Weather Dashboard (AQI.in style) with Express backend for Vercel",
  "main": "api/index.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "build": "node --check server/server.js && node --check script1.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "netlify-cli": "^27.1.0"
  }
}
